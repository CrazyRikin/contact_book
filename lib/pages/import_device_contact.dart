import 'package:flutter/material.dart';

class DeviceContacts extends StatelessWidget {
  const DeviceContacts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: const Center(
          child: Text("Device Contacts"),
        ));
  }
}
