import 'package:flutter/material.dart';

class ExportContacts extends StatelessWidget {
  const ExportContacts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: const Center(
          child: Text(" Export Contacts"),
        ));
  }
}
